# [项目名] —— 一句话 pitch(不超过一句,说清"这是什么、厉害在哪")

> 顶部放 1~3 张截图或一张 GIF(1280×720 左右),这是别人打开仓库后 3 秒内决定要不要看的关键。

## 特性

- ⭐ 亮点 1(可玩 / 可用,实实在在做过并跑通的事)
- ⭐ 亮点 2
- ⭐ 亮点 3

## 快速开始

需要环境:例如 `Python 3.9+`

```bash
# 进入到本项目的子目录(monorepo 结构)
cd projects/[目录名]

pip install -r requirements.txt
python main.py
```

## 目录结构

```
[目录名]/
├── main.py        # 入口
├── xxx.py         # 核心逻辑
├── web/           # 前端
└── requirements.txt
```

## 技术栈

- 语言 / 框架 / 依赖(GitHub API、requests、pywebview……)

## 开发记录(dev log)

> 这是 vibe coding 项目集的灵魂,记录"怎么一步步聊出来的",人比代码更值得读。

### 迭代过程

- **第 1 轮**:最初需求 → 做了什么 → 结果(哪里能跑、哪里不对)
- **第 2 轮**:根据实测发现的问题,继续对话调整 → 结果
- **第 3 轮**:……(每条尽量写:需求 / 动作 / 实测结论)

### 踩过的坑

- 坑 1 及解决方式
- 坑 2 及解决方式

### 学到的东西

- 一两句本次学到的最有价值的东西

## 许可证

本仓库(项目集)统一使用 [MIT](../LICENSE) 许可;如本目录内容来自其他开源项目,请在下方注明版权归属。